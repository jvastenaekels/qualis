# Semgrep ignore list — paths covered by inline `# nosec` rationale or
# previously triaged in the 2026-05-03 audit (Wave 1).
#
# Each entry must remain justified; review when files change shape.

# F-02-008 (Wave 1) — env-gated dev/test router. Uses sqlalchemy.text(f"...")
# with hardcoded table-name literals only; never untrusted input. Already
# annotated with `# nosec` for bandit. Triage at
# docs/audits/2026-05-03-comprehensive-security-audit/02-scanner-pass.md
backend/app/routers/test.py

# F-02-009 (Wave 1) — operational migration scripts. Run by maintainers
# only; no untrusted input. Same triage as test.py.
backend/scripts/migrate_projects_config.py
backend/scripts/validate_migration.py

# Generated API client — semgrep timed out on the express-ssrf rule for
# this file in CI. Generated by openapi-typescript-codegen; not authored
# code. Excluded for performance + relevance.
frontend/src/api/generated.ts
