{
  "name": "qualis-root",
  "version": "0.8.0",
  "private": true,
  "scripts": {
    "build": "cd src/frontend && npm install --include=dev && npm run build",
    "postinstall": "echo 'Postinstall root skipping'"
  },
  "engines": {
    "node": "26.x"
  },
  "dependencies": {
    "immer": "^11.1.3"
  }
}
