{
  "name": "open-q-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "postinstall": "echo 'Postinstall root skipping'"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "immer": "^11.1.3"
  }
}
